import random

number = random.randint(1, 10)
guess = int(input("Guess a number between 1 and 10: "))

if guess == number:
  print("Well done! You guessed right!")
else:
  print(f"Wrong! The number you were looking for was {number}.")  
