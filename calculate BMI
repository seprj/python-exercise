weight = float(input("Your weight (Kg): "))
height = float(input("Your height (m) : "))

bmi = weight / (height ** 2)

if bmi < 18.5:
    status = "Underweight"
elif 18.5 <= bmi < 25:
    status = "Normal"
else:
    status = "Overweight"

print(f"Your BMI : {bmi:.1f} ({status})")
